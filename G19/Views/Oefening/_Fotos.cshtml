@model G19.Models.Oefening

@{
    ViewData["Title"] = "_Fotos";
}
<head>
    <link rel="stylesheet" href="~/css/textPartial.css" />
</head>

@foreach (var image in @Model.Images) {
<div class="container">
    <div class="card">@image.Images</div>
    <div class="card">@image.Images</div>
    <div class="card">@image.Images</div>
    <div class="card">@image.Images</div>
    <div class="card">@image.Images</div>
    
</div>
}

